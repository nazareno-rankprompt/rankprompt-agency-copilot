# secrets
.env
.rp.env

# working memory / client data (each agency's own)
profile.md
out/
clients/

# agency white-label branding (each agency's own)
branding.json
logos/agency-logo.png
logos/agency-logo.svg

# generated deliverables
*.pdf
# ...but keep the reference example audits (real client audits, shipped as gold-standard references)
!examples/**

# os
.DS_Store
